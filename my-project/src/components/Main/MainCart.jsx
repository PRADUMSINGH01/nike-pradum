import React from 'react'

const MainCart = ({bg,shadow}) => {
  return (
 <div className={` w-[20rem] rounded-xl h-[16rem] relative bg-${bg} shadow-lg ${shadow} cursor-pointer  transition-all duration-700 ease-in-out hover:scale-105`}>1</div>
  )
}

export default MainCart